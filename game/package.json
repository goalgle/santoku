{
  "name": "santoku-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "산토쿠 전술 레이어 — 진짜 코드베이스(1단계~). 헤드리스 결정적 sim + (추후) PixiJS 렌더.",
  "scripts": {
    "sim": "tsx src/headless.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
