{
  "name": "santoku-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "산토쿠 전술 레이어 — 진짜 코드베이스. 헤드리스 결정적 sim + PixiJS 렌더(sim 상태를 그림).",
  "scripts": {
    "sim": "tsx src/headless.ts",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
