# CLAUDE.md — 프로젝트 컨텍스트 (가제: Ancient Average / repo: santoku)

> 이 파일은 **다른 PC/새 세션에서 작업을 이어받기 위한 핸드오프 문서**다.
> Claude Code 세션 기록은 머신 로컬이라 동기화되지 않으므로, 맥락은 이 파일과 `doc/`로 잇는다.
> 새 세션을 시작하면 먼저 이 파일과 [`doc/README.md`](./doc/README.md)를 읽을 것.

## 프로젝트 한 줄 요약

삼국지 배경의 **토탈워류 전략 게임**. 웹 기반(모바일 우선 → PC), **1인 소규모 프로토타입**.
현재 단계: **전술 우선 구현 중** — `spike-0/`(0단계 렌더 스파이크, 종결) → **`game/`(진짜 코드베이스, 1단계 A sim 코어 완료)**. 스펙은 `doc/` 참고.

## 핵심 설계 (확정된 것)

- **3개 레이어 + 연속 줌**
  - **경영**: 턴제, 전체 지도. 내정·외교·부대 편성·이동.
  - **전략**: 실시간, **Hex 전장**. 부대 단위 지휘. **교전은 자동 계산**(능력치·전방·지형·장수·협공). *병종간 상성 미도입.*
  - **전술**: 실시간, 특정 교전을 **확대**해 유저가 직접 개입. 장수·개별 병종·지형 연출이 보임.
- **핵심 메커니즘**
  - **액티브 포즈**: 유저가 개입(명령)하는 순간 전체 정지. 판단력 > 반응속도.
  - **이중 시계**: 전략은 빠르게, 전술은 느리게 흐름(`TIME_RATIO`). 둘은 공유 X, 비율로 분리.
  - **주의의 안개**: 전술 몰입 중 벌어진 전략 상황을 알림으로 공유(예: "A부대 후퇴 중").
- **유닛**: Hex 6면 + **전방(facing)**, 한 타일 한 유닛. **병종간 상성 없음**(능력치·지형·facing으로 차별화). **사기 붕괴**로 결판.
- **기술 방향(미확정)**: PixiJS + TypeScript + Vite, 클라이언트 온리. 줌인 교전 1건만 정밀 시뮬.

## 문서 맵 (`doc/`)

상세는 [`doc/README.md`](./doc/README.md) 인덱스 참고. 01 개요 / 02 경영 / 03 전략 / 04 전술 /
05 기술 / 06 프로토타입 / **07 엔티티 데이터(🟡 작업 중)**.

## 현재 상태 & 다음 작업

- [x] 3레이어 구조·전투 흐름·이중 시계 정리 완료
- [x] **장수 시스템 1차 골격 확정** (2026-06-30 세션) — 아래 "확정 요약" 참고
- [x] **전술 표현·지형 방향 확정** (2026-07-01) — 연속 공간+대형 덩어리, 지형 혼합 규칙
- [x] **병종 시스템·교전 데미지 모델 1차 확정** (2026-07-02~03) — 상성 제거, 저지/공속/치명율, 접전 폭 병목 (아래 "확정 요약")
- [x] **개발 순서 = 전술 우선 확정** (2026-07-03) — 전술 수직 슬라이스 먼저, 그 위에 전략·경영. `doc/06` 재정렬.
- [~] **전술 슬라이스 v0 구성** (`doc/04` 4.8) — 1:1 대칭, 편성 **방패1000·창600·궁400·기병200+장수**(부대 2200), 지형 = 랜덤 3종 템플릿(T1 평야회전/T2 구릉지/T3 애로), 장기식 배치, 개입 **이동/배치+돌격+방어전념**.
  **고지→저지 보정 = 공격+20%/이동+30%/궁 사거리+30%.** **승리 정도 = 잔존 ≥80% 대승리 / ≥50% 승리 / <50% 안타까운 승리.** **남음: 궁/기병 특수개입(①)만.**
- [x] **유닛 이동/배치 명령**(`doc/04` 4.5.2) — 액티브 포즈 → 병종별 위치 지정 → 재개 이동. 방패 방어위치도 동일.
- [x] **이동·회전 규칙**(`doc/04` 4.5.3) — 이동속도=이동력, 회전속도=회전. 기본 제자리 회전(대열 유지). **덩어리 이동=모임→도착지 펼침**(이동 중 전투효율↓). 명령반경 넓음·병종 이탈 가능(반경 밖=명령 선택 불가, 목표 지점은 반경 무관). **기병**: 정지 시 제자리 회전(멈추면 약화)/이동 중 **선회**(반경≈장수 20배, 히트앤런·재돌격 필수). **가속 최대속도(기본 2초)→공격 A→S·방어 보정**(창병 저지로 감속 시 무효, 내리막 가속).
- [x] **전술 구현 설계**(`doc/05` 5.6) — 헤드리스 결정적 sim + PixiJS 렌더 분리, 모듈 4(data/sim/render/input), 데이터 모델(Battle/Unit/Cohort/General/Flag), 틱 루프, **핵심 난제 3(접전 폭 기하·덩어리 이동·기병)**, 빌드 순서.
- [x] **0단계 스파이크 GO (종결)** — `spike-0/` (PixiJS v8+Vite+TS, 던져버리는 실험). 덩어리 렌더·facing·이동(모임/펼침)·사상자(좌우 실루엣+전면 팝)·도주 흩어짐·핀치줌·**연속 애니(○□△★)·지형 배경** 구현. **데스크탑 120 / 모바일 60fps(4000 스프라이트+애니) 확인** → 렌더 리스크 종결.
  - 확인된 원리: **시뮬 비용=O(부대 수), 렌더=O(스프라이트)** — 스프라이트 많아도 전투 연산 부하 극소. 흩어짐·전면 팝·애니 전부 **순수 렌더 표현**(sim에 되먹임 X). 스프라이트에 로직 금지.
  - **라이브 배포: https://goalgle.github.io/santoku/** — `main`에 `spike-0/**` 푸시 시 GitHub Actions(`.github/workflows/deploy.yml`) 자동 빌드·배포.
- [x] **1단계 A — sim 코어 (헤드리스)** — `game/` (진짜 코드베이스, spike-0와 별개). 데이터 모델(Battle/Unit/Cohort/General/Flag)·등급→배율·병종 스탯·시드 RNG(결정성)·스냅샷 로드·고정 틱. `npm run sim` 검증(명령반경 210·폭·결정성 OK).
- [x] **1단계 B — 이동/배치 (헤드리스)** — `game/`. 이동력→속도·회전→회전속도, **모임→펼침**, **명령반경 gating**, **기병 가속(2초)·선회(반경=장수20배)**.
- [x] **1단계 C — 접전 폭·근접 전투 (헤드리스)** — `game/`. **접전 폭 = 전면 선분 투영 겹침**(05 5.6.5 난제 구현), 대형 배치, 근접 피해=접전폭×공속×공/방, **치명율로 전사/부상 분배**, 병력↓→폭↓. 검증: 전사 7%(=치명율 C) ✓.
- [x] **1단계 D — 궁병 사격·사기·도주·정도 (헤드리스)** — `game/`. **궁병 사격**(정지·사거리 내 전원, **근접 접촉 중 불가**=근접 취약), **사기**(접전 사상 기반 하락, 0→도주), **도주 페이즈**(10s 속수무책), **종료+승리 정도**(잔존 ≥80 대승리/≥50 승리/그외 안타까운 승리). 검증: 궁 사격 94% 부상 ✓, 창vs궁 → 사기0→도주→대승리 ✓. **전투 파이프라인 완결(deploy→engage→rout→ended).**
- [x] **1단계 E — 기병 charge·장수 (헤드리스)** — `game/`. **기병 charge**(최대속도→공격 A→S·방어 보정, **저지로 감속=창>방패 카운터**; charge 임팩트 데미지 튜닝 TODO), **장수**(무력 일기토, HP0→휴식·군기 리젠·재출진, 근접 10초→사기+5, **생사 판정** HP≥50% 대기/미만 부상·사망). 검증 ✓.
- [x] **1단계 F — 지형 (헤드리스)** — `game/` (data/terrain.ts 템플릿 3종). **고지→저지 공격 +20%**(검증 B/A=1.20 ✓)·**궁 사거리 +30%**, **애로 병목**(접전 폭 100→30, 손실 32% ✓). **★ 1단계 A~F 전투 sim 완결(헤드리스).**
- [x] **시나리오/Director 구조** — `game/` (scenario.ts·director.ts·scenarios.ts). 컷신·테스트를 **타임라인(언제·무엇)** 으로 스크립트(병종·장수·지형 제어 op). `new Director(scn)`→`step(dt)`→`battle`. 액티브 포즈=paused. **렌더가 그대로 재사용할 재생 구조.** `npm run sim [advance|charge|duel|hill|defile]`.
- [~] **렌더(PixiJS) 붙이기 (1차)** — `game/src/render`(blobView·camera)·`game/src/web/main.ts`. **sim(Director) 상태를 스프라이트로 그림** — cohort anchor/facing/폭/사상 → 덩어리, 장수/군기 마커, 지형, 핀치줌, space=액티브 포즈. `npm run dev`(폰=LAN, `?s=시나리오키`). **배포 spike-0→game 전환**, 라이브 https://goalgle.github.io/santoku/ 에서 전투 재생.
  - **일기토 결투장 연출**(렌더 전용): 양측 장수 근접 시 병사들이 물러나 원을 만들어 둘러쌈, 접전 지속(meleeTime)에 원 성장, 장수는 서로 돎. (`?s=duel`)
  - **컷신/시나리오 작성 가이드 → `game/CUTSCENES.md`** (scene 빌더·op·좌표/인덱스 규칙).
  - **남음**: 개입 UI(명령 입력)·사망 팝/애니 이식(spike-0)·전면 진격 시나리오·scale 튜닝·장수 부드러운 이동.
- [ ] **07 데이터 채우기** — 등급→계수 환산·부대 규모 등 **수치** 1차안 (🟡 골격 확정, 수치 OPEN)

### 확정 요약 (장수 시스템 — 2026-06-30~07-01)
- **능력치 6종**: 통·무·지·정·매·운 (0~100). `통솔→지휘반경`, `지력→계략범위` 대칭.
- **운=시드 RNG**(결정성 유지, `doc/05`). 매력·정치는 프로토타입 **데이터만**(미사용).
- **장수 = 버프+일기토 하이브리드**: HP 보유, 출진/휴식/**대기**, **부대군기**로 리젠.
- **부대군기**: 이동=보병 1/2. **부서지지 않으면** ①명령(반경=통솔 비례) ②HP회복·리젠 포인트.
  **부서지면** 명령·리젠 불가 + 사기↓.
- **전투 종료·도주 (갱신 2026-07-03)**: **한쪽 사기 0 → 종료**(구 "사기0 & 장수HP0" 대체). 패배 유닛 **부상병 데리고 도주 → 화면 끝 도달**(그동안 피격=속수무책, **추격·퇴로 자동**), **도주 상한 ≈10초** 초과 시 즉시 종료.
  **장수 생사 = 도주 종료 시점 HP:** 50%+ → **'대기'**(전략 재편성 유닛 장수로 재임명 가능) / 50% 미만 → **부상·사망**(전략 기용 불가). 도주 유닛 **대장군기 집결**(소멸 아님) → 여분 장수(=대기) 시 **저사기 재편성**. ("해산" 폐기)
- **계략**(=장수 특수기, 커맨드와 구분): 제자리 작전P 차징→피격 시 취소, 범위=지휘반경+지력.
- **전술계략 간파**: 방어측 지략 주사위(대성공=종류+타겟/성공=종류/실패). **간파·위보는 장수 특기**로
  차징 시 주사위 ±보정(간파=+, 위보=상대−). **거짓정보 없음**(복잡도로 미도입).
- **사기**(`doc/03` 3.6.1): 0~100 시작 50, 증감 요인 표, 데미지 작용, ≤20 명령 반응 실패.
- **장수 특성 = 패시브**(조건 발동, 기본 규칙 완화형; `doc/07` 7.1). 후보 5종:
  고무(근접전투 지속→사기, 10→7초·2회) / 간파·위보(계략 간파 주사위 ±) / 추격·퇴로(도주).
  기본 규칙: 장수 **근접전투 10초 지속 → 전술 페이즈당 사기 1회↑**.

### 확정 요약 (전술 표현·지형 — 2026-07-01)
- **전술 = 연속 공간(Hex 아님) + 대형 덩어리**(`doc/04` 4.2.1): 장수=단일 에이전트,
  병종=그룹 단위 시뮬(앵커+대형 슬롯 스프라이트+노이즈). facing=연속 각도.
- **레이어 격리 & 최소 인터페이스**(`doc/04` 4.6, `doc/05` 5.4): 전술은 독립 레이어.
  **진입=전략의 교전 스냅샷 / 복귀=승·패+승리의 정도**(정도→전략 유닛 사기). 세부 상태 되돌림·위치 변환 **없음**.
- **협공 & 동적 구성**(`doc/04` 4.6): 전술은 1:1만 아님. **2:1 협공**은 Hex 인접 방향대로 진입.
  전술 진행 중 전략 실시간 연동으로 **구성 변동(1:1↔2:1)**. 시간은 `TIME_RATIO` 공유+알림.
- **전략 유닛 사기 원천**(`doc/03` 3.6.1): 보급/후퇴·지원 소식 + **전술 승리의 정도** + **전략 계략 성공**.
- **명령·계략 3종 체계**(`doc/04` 4.5.1): **전략계략**(전략 화면만, 경영서 전투 전 부여, 타이밍 유저) /
  **전술계략**(장수 작전P 차징, 범위=명령반경+지력, 간파·위보 / =기존 '계략') /
  **전술명령**(부대군기 있으면 가능, 장수 없어도 발동; 예 돌격·기마저지·방어전념).
- **지형**(`doc/07` 7.3): 항목화(평지/개천/강/언덕/산악/숲/도하/협곡). **전략→전술 혼합 = 대표 지형 ≥60% + 잔여 혼합**, 시드 절차생성.
- **병종**(`doc/07` 7.2): 한 유닛=여러 병종. **로스터 5종** = 방패병(검병)·창병·궁병·기병·깃발병.
  능력치(등급 S/A/B/C/D+특수 E; 장수는 0~100 수치) = 공격/방어/**병수(HP)**/**부상병**/이동/**회전**/사거리/저지/**공속**/**치명율**.
  **저지 확정:** 창·궁 A / 방패 B / 기병 E(돌파) / 깃발 D. **공속 확정:** 방패 C / 창 A / 궁 B / 기병 S. **치명율 초안:** 기병 A / 창 B / 방패 C / 궁 D.
  남은 제안값(˟: 방패 방어·기병 사거리·깃발 사거리·치명율 초안) 확정은 OPEN.
  **깃발병 = 부대군기 유닛**(확정). **병종간 상성 미도입(확정)** — 능력치·지형·facing으로 차별화. **병종×지형 매트릭스 1차 확정**.
  **궁병 이동사격 없음**(정지 시만 사격). **방패병 방향별 방어 = 전100/측70/후30**.
- **교전 데미지 모델**(`doc/03` 3.6.2, 1차 골격): **덩어리 시뮬**. **두 병목** = ①접전 폭(근접: 전면=생존÷두께10, 공격횟수=실효전면÷10; 궁은 전열병목 없이 사거리 내 전원 사격) ②공속(병종계수).
  **치명율**로 피해를 전사(영구)/부상(복구) 분배. 기병 돌파는 전열 우회, **창병 저지 A로 붙잡히면 무효**(상성표 없이 카운터 발생).
- **등급 배율 확정**(2026-07-03): S/A/B/C/D/E = **100/90/80/70/60/50** (10점 등차). 대립 계산은 내÷상대. **치명율만 ÷10 → 10/9/8/7/6/5%**.
- **장수 반경 환산**(1차, `doc/03` 3.2.1): 통솔·지력 **100 → 장수유닛 30배 반경**(선형). 계략 범위 = 지휘반경 + 지력 기여분.
- **대열·사상자**(`doc/04` 4.2.1): 최소두께 10, 최대 폭 확산(폭=병력÷두께, 1000명이 500명의 2배 폭). 지형이 폭 제한 시 두께↑. **사상자는 좌우→중앙 수렴**(병력↓→폭↓→공격횟수↓).
- **겹침 규칙**(`doc/04` 4.2.1, 개정 2026-07-08): **같은 진영=겹침 허용**(방패 앞·창 바로 뒤 촘촘히), **반대 진영=전면 가장자리만 겹침**(그 이상 통과 불가). **예외 돌파**=기병 charge·장수(느리지만). sim에 반대진영 충돌 해소 구현.
- **부상병 회복 = 전략 화면 전용**(확정): 전략계략 **후송·의무반**/재편성. 전술 중 복구 없음. 장수 HP 회복(전술 휴식)과는 별개.

### 아직 열린 핵심 결정 / OPEN
- 시나리오: 정통 삼국지 vs 가상 군웅할거 (`doc/01` 1.6)
- `TIME_RATIO` 초기값 (`doc/04` 4.7)
- **전략계략 종류·간파** — 골격만 확정(경영 부여·전략 화면·타이밍 유저, `doc/04` 4.5.1). **구체 종류/효과는 전략 레이어 구체화 후로 보류**(사용자 결정 2026-07-02).
- **승리 '정도' 단계·환산** — 잔존 전력·사기 격차 → 대승/승/신승? (`doc/04` 4.6)
- **협공 전술 배치 규칙** 상세 (`doc/04` 4.6)
- **장수 세부 수치 OPEN** (`doc/03`·`doc/07`): 통솔→반경/지력→계략범위 환산식, 사기 감소율·사기→데미지 환산,
  작전P 차징·취소 수치, 간파 판정식, 군기 HP/방어/파괴조건, **재편성 임계 수·재편성 시작 사기값·여분 장수 배정**.
- **도주 vs 철수** 처리 차이 (`doc/03` 3.6.1 OPEN).
- 🟡 **장수별 특수기**('위보'·'고무' 등) — 별도 상세화 예정 (`doc/07` 7.1).

## 작업 규칙 (convention)

- 문서는 **living document** — 결정이 바뀌면 관련 `doc/*`와 이 파일을 함께 갱신.
- 밸런싱 수치는 **코드가 아니라 데이터로** 둔다(JSON/TS 분리).
- 커밋 후 `git push`로 동기화(이게 PC 간 작업 이어가는 통로).
- 문서는 한국어로 작성.

## 다른 PC에서 이어받는 법

1. `git clone https://github.com/goalgle/santoku.git && cd santoku`
2. 그 폴더에서 `claude` 실행 → 이 `CLAUDE.md`와 `doc/`를 읽고 맥락 복원.
3. (선택) 이전 대화 흐름까지 그대로 옮기고 싶으면, 기존 PC의
   `~/.claude/projects/<프로젝트경로>/<세션id>.jsonl`을 새 PC의 동일 경로에 복사 →
   `claude --resume`로 선택. ⚠️ 비공식 방법이며, 절대경로가 같아야 하고 버전에 따라 깨질 수 있음.
   → **권장은 이 파일 기반으로 새 세션을 시작하는 것.**
